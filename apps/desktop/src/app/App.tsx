// Root application shell. Handles top-level view routing.
// Views: Builder, LiveRun, Replay, Library. See ARCHITECTURE.md §10.

export function App() {
  return (
    <div className="app">
      {/* View routing goes here */}
      <p>Agent Arcade — skeleton</p>
    </div>
  );
}
